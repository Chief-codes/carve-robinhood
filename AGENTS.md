# Carve

This project is separate from The Analyst, RobinhoodTelegramRadar and all older apps. Do not modify their files, services, keys, filters or deployments.

Only the owning agent edits this Site checkout or uses Sites tools. Research/contract work may be returned as reviewed artifacts from outside this checkout.

Product: Robinhood Chain token launches with actual onchain image, animation, audio and self-contained website bytes. Adapt Scribe.ong functionality with original implementation and branding. Never present hashes, offchain-only files or IPFS URLs as full onchain storage. Never invent launched tokens, burn stats, deployment addresses or successful transactions.

Mainnet writes require an explicit wallet signature by the user for the exact reviewed transaction. Build, simulate and test locally first. Preserve drafts and resumable uploads; never store private keys. EIP-6963 provider selection must target the chosen wallet. Arbitrary uploaded HTML must be sandboxed and served with a restrictive CSP on a distinct origin or unique opaque origin.

Read docs/RESEARCH.md and docs/STATUS.md for current evidence and blockers. Contracts are not deployed until receipts and runtime code are verified. Do not claim all utilities are live before end-to-end execution. Any protocol buyback needs the platform token, revenue configuration, liquidity and funded automation; never fake those.
